//You have no control over when the garbage collections run. It all happens in the
//background. It basically deallocates the memory of  any unused variable or constants.

const circly = {};
console.log(circle);
